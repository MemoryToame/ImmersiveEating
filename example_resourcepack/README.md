# Example Resource Pack

Replace `modid:example_item` in `assets/food/definitions/example_item.json` with the real item ID before using this pack.

The model, animation, and texture use the shared prefix `example_item`. The sound file is `example_eating.ogg` and is mapped from the `eating` animation sound keyframe.
